#pragma once

#include <string>
#include <vector>
#include <unordered_map>
#include <cstdint>

namespace inf {

void loadInf(const std::string& filePath);

void loadAtx(const std::string& filePath);

} // namespace inf
